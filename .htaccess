<IfModule mod_rewrite.c>
    RewriteEngine On

    # If the file or directory exists, don’t rewrite it
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Rewrite all other URLs to index.php
    RewriteRule ^(.*)$ index.php/$1 [L]

</IfModule>
